const usernameTextBox = 'input[placeholder="Username"]';
const passwordTextBox = 'input[placeholder="Password"]';
const loginButton = '.oxd-button';
const errorMessage = '.oxd-alert-content > .oxd-text';

export
{
    usernameTextBox,
    passwordTextBox,
    loginButton,
    errorMessage
}